module.exports = 
	createAll: true # 默认生成所有的定制接口的代码，需要谨慎，如果已经生成的，将会重新生成
	codePath = 'E:/WORKSPACE1/imm/java/' #代码路径，代码将生成在这个工程下
	interfaceNames = [ '零售户数据查询' ] #createAll为false的情况下，自己选择已经定义的接口名，来生成java代码